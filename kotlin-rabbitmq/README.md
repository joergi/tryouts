# RabbitMQ with Kotlin and Spring Boot

to test:
```shell
curl --location --request POST 'http://localhost:8080/data/send' \
--header 'Content-Type: application/json' \
--data-raw '{
    "message": "this is a test"
}'
```
